# /task-search

Search tasks by a text query.

## Arguments

- `query` (required): The search string to match against task titles and metadata.

## Steps

1. Run:

   ```
   dev-workflow task search "<query>"
   ```

2. Display the output to the user.
